export class Sort {
  static MEDIA = { items: ["createdAt", "updatedAt"], default: "createdAt" };
  static CATEGORY = {
    items: ["name", "productCount", "createdAt", "updatedAt"],
    default: "createdAt",
  };
}
